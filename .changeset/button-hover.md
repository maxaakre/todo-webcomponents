---
"@maxaakre/ui": patch
---

`ui-button` has a visible hover state in every variant, in both themes. Outlined (`secondary`) and `ghost` buttons previously showed no visible change. Hover is off for disabled buttons and on touch-only devices.
