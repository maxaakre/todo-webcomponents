---
"@maxaakre/ui": minor
---

Add `ui-tabs`, `ui-tab` and `ui-tab-panel`: the WAI-ARIA tabs pattern with manual activation and roving tabindex. Tabs and panels are light-DOM children so `aria-controls` works.
