---
"@maxaakre/ui": minor
---

Add `@maxaakre/ui/react`: JSX types for React 19, generated from `custom-elements.json`. `import type {} from '@maxaakre/ui/react'` types every element's properties and custom events.
